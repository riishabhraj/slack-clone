'use client';

/**
 * CallDebugger - DISABLED
 *
 * This component has been completely disabled to eliminate debug logs in production.
 * It returns null in all environments to prevent any debug functionality.
 */
export function CallDebugger() {
    // Return null to render nothing
    return null;
}
